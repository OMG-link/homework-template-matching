编写一个python脚本，其接受两个参数：jpg文件路径和输出路径。

其功能是：将jpg文件转化为灰度图，并输出到输出路径中。

输出的是一个文本文件，文件格式如下：
```
<图片高度 n> <图片宽度 m>
<像素(1,1)> <像素(1,2)> ... <像素(1,m)>
<像素(2,1)> <像素(2,2)> ... <像素(2,m)>
...
<像素(n,1)> <像素(n,2)> ... <像素(n,m)>
```
